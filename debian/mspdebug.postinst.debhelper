# Automatically added by dh_installudev
if [ "$1" = configure ]; then
	if [ -e "/etc/udev/rules.d/92_mspdebug.rules" ]; then
		echo "Preserving user changes to /etc/udev/rules.d/92-mspdebug.rules ..."
		if [ -e "/etc/udev/rules.d/92-mspdebug.rules" ]; then
			mv -f "/etc/udev/rules.d/92-mspdebug.rules" "/etc/udev/rules.d/92-mspdebug.rules.dpkg-new"
		fi
		mv -f "/etc/udev/rules.d/92_mspdebug.rules" "/etc/udev/rules.d/92-mspdebug.rules"
	fi
fi
# End automatically added section
